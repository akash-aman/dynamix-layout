import { Type } from '../types';

export const AreaType = {
	top: Type.Row,
	left: Type.Column,
	right: Type.Column,
	bottom: Type.Row,
	cover: Type.Tab
}

export const AreaIndex = {
	top: 0,
	left: 0,
	right: 1,
	bottom: 1,
	cover : 0
}